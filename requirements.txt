# Рантайм бота
aiogram>=3.4,<4
anthropic>=0.39
pydantic>=2.5

# KV-хранилище пользователей/попыток/сессий на Vercel (Upstash Redis, REST)
upstash-redis>=1.1

# Лингвистика (сборка банка + детекторы проверки)
pymorphy3>=2.0
razdel>=0.5

# Выверка по Викисловарю (офлайн)
requests>=2.31

# Тесты
pytest>=7.4
